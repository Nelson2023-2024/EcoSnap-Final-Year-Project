services:
  nginx:
    ports:
      - 80:80

  ecosnap-backend:
    build:
      args:
        - NODE_ENV=production
    environment:
      - NODE_ENV=production
    command: ["npm", "run", "start"]

  